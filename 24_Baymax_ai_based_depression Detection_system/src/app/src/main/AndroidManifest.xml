<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.depression">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Depression">
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main2"
            android:theme="@style/Theme.Depression.NoActionBar"/>
        <activity
            android:name=".ui.OtpActvity"
            android:label="@string/otp_verification"
            android:theme="@style/Theme.Depression.NoActionBar"/>
        <activity
            android:name=".ui.LoginActivity"
            android:theme="@style/FullScreenTheme" />
        <activity
            android:name=".ui.RegisterActivity"
            android:theme="@style/FullScreenTheme" />
        <activity
            android:name=".ui.SplashActivity"
            android:theme="@style/FullScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.tyagiabhinav.dialogflowchatlibrary.ChatbotActivity"
            android:launchMode="singleTask"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
    </application>

</manifest>